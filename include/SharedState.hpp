#pragma once
#ifndef SHARED_STATE_H
#define SHARED_STATE_H

// These 'extern' declarations tell your other files that these 
// variables exist and are created in main.cpp
extern bool acceptedShadyDeal;
extern bool possessesStolenItem;
extern bool itemIsHidden;

#endif